{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../puhelinluettelo-frontend && npm run build && cp -r dist ../puhelinluettelo-backend",
    "deploy:full": "npm run build:ui && git add . && git commit --allow-empty-message --no-edit && git push",
    "lint": "eslint ."
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongodb": "^6.18.0",
    "mongoose": "^8.16.5",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "eslint": "^9.32.0",
    "globals": "^16.3.0"
  }
}
